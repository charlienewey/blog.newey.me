#! /usr/bin/env bash

set -e

bundle exec jekyll build
bundle exec htmlproofer --url-ignore "/#respond/" \
                        --http-status-ignore 0 \
                        --check-html \
                        --check-opengraph \
                        ./_site
